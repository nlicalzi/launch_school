# Exercise 5, Hashes, Intro to Programming, Launch School

p "We could use the 'has_value?' method to find if a hash contains a specific value."

pets = {cat: "Bean", dog: "Beatrice"}

p "Given the hash 'pets': #{pets}, we can evaluate if it contains 'Bean' like so: " +
  "pets.has_value?('Bean'): #{pets.has_value?('Bean')}"
